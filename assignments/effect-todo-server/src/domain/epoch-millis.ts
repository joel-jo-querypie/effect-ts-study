import { Schema } from "effect";

export const EpochMillis = Schema.Number.pipe(
  Schema.int(),
  Schema.nonNegative(),
  Schema.brand("EpochMillis"),
);
export type EpochMillis = Schema.Schema.Type<typeof EpochMillis>;

export const epochMillisFromNumber = (value: number): EpochMillis =>
  Schema.decodeUnknownSync(EpochMillis)(value);

/**
 * 개발 중 코드 파일 상단이나 디자인 가이드 주석에 바로 복사해서 붙여넣을 수 있도록, 핵심만 명확하게 요약해 드립니다.

 코드 가독성을 위해 불필요한 수식어를 걷어내고, **실무진의 핵심 논쟁점과 결정 기준** 위주로 슬림하게 정리했습니다.

 ---

 ```typescript
 /**
  * "우리는 DDD 학자가 아닌 실무 개발자다."
  * 기계적으로 VO를 남용하는 대신, 아래 3가지 기준과 예외 케이스를 체크리스트로 삼아 결정합니다.
  *
  * ----------------------------------------------------------------------------
  * 1. VO 도입 검토 기준 (3가지)
  * ----------------------------------------------------------------------------
  *  ① 생성 시점 검증 (Validation) 필수 여부
  *     - 객체가 생성되는 '그 순간' 무조건 비즈니스 규칙을 통과해야 하는가?
  *     - 생성에 성공했다는 것 자체가 '올바른 상태'임을 보장해야 한다면 VO로 승격.
  *     (e.g., Email, Password 등)
  *
  *  ② 사이드 이펙트 제거 및 불변성 (Immutability) 보장
  *     - 값 변경 시 내부 상태를 수정하지 않고 항상 새로운 객체를 반환(Copy & New)하는가?
  *     - 외부 상태(시간, API 등)에 의존하지 않는 순수 함수(Pure Function)로만 구성되어
  *       개발자의 인지 부하(뇌 용량)를 줄여줄 수 있다면 VO로 설계.
  *
  *  ③ 엔티티의 도메인 로직 분산 (e.g., Money)
  *     - 엔티티의 필드와 비즈니스 로직(환율, 소수점 절사 등)이 너무 무거워 찢어내야 하는가?
  *     - 해당 속성이 고유한 비즈니스 계산식을 품고 있다면 전용 VO로 위임.
  *
  * ----------------------------------------------------------------------------
  * 2. 실무적 타협안 및 주의점 (Anti-Pattern 방지)
  * ----------------------------------------------------------------------------
  *  ⚠️ 무지성 VO 남용 경계
  *     - 도메인 로직이 없는 범용 데이터이거나, 단순히 출력/기록용이라면
  *       억지로 VO 껍데기를 씌우지 말고 원시 타입(string, number)을 유지할 것.
  *
  *  ⚠️ 생성 권한의 꼬임 방지 (의존성 오염)
  *     - VO 생성 시 외부 API나 환율 정보 등 엔티티가 몰라야 할 지식이 필요하다면,
  *       엔티티 내부가 아닌 서비스 레이어나 팩토리에서 VO를 완제품으로 만들어 엔티티에 주입할 것.
  *       (e.g., product.updatePrice(localMoney) 꼴로 설계)
  *
  * ----------------------------------------------------------------------------
  * 3. 10초 만에 끝내는 의사결정 트리 (Decision Tree)
  * ----------------------------------------------------------------------------
  *  Q1. 생성 시점의 검증 조건이 도메인 필수 규칙인가?
  *      ├── YES ──> [VO로 승격] (e.g., Email, Password)
  *      └── NO  ──> 원시 타입(string, number) 유지
  *
  *  Q2. 값의 연산에 고유 비즈니스 사정(환율, 절사 등)이 개입하는가?
  *      ├── YES ──> [VO로 승격] (e.g., Money, Period)
  *      └── NO  ──> 기본 라이브러리 타입(Date, BigDecimal) 사용
  *
  *  Q3. 인터페이스나 레이어 통신 시 데이터 오염을 격리해야 하는가?
  *      ├── YES ──> 통신용 값 객체/DTO(Command, Request) 도입
  *      └── NO  ──> 엔티티 직접 노출 지양 (필요 시 내부 모델 사용)
  * ============================================================================
  * /

 ```
 */
